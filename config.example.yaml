cleartext:
  enabled: true
  listen_addr: ":8080"

algo: rr

upstreams:
  - http://localhost:9000
  - http://localhost:9001

tls:
  enabled: false
  listen_addr: ":8443"
  cert: "certs/cert.pem"
  key: "certs/key.pem"
